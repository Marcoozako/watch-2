<h2><%= @list.name %></h2>

<h3>commentaires</h3>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li> <%= bookmark.comment %>
    <%= link_to 'Supp', bookmark_path(bookmark.id), data: {turbo_method: :delete} %> </li>
  <% end %>
</ul>

<h4>Son film associé soon...</h4>
<ul>
<% @list.movies.each do |movie| %>
  <li><%= movie.title %></li>
<% end %>
</ul>
<%= link_to 'Add a Review', new_list_bookmark_path(@list.id) %>

<%= link_to 'retour accueil', root_path, class:'btn btn-primary' %>
